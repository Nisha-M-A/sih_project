cmake_minimum_required(VERSION 3.8)
project(FaceRecognitionBackend)

# Add your dlib build instructions, e.g.
add_subdirectory(dlib)
# find_package(OpenCV REQUIRED)
# include_directories(${OpenCV_INCLUDE_DIRS})

# If you have a C++ extension/module to build, add it here
# add_executable(recognize_cpp src/recognize.cpp)
# target_link_libraries(recognize_cpp dlib::dlib ${OpenCV_LIBS})
