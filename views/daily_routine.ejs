<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Teacher & Student Portal</title>
<style>
:root {
  --bg:#f4f7fb;
  --card:#fff;
  --accent:#0b63d8;
  --muted:#6b7280;
  --radius:12px;
  font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, Arial;
}

body {
  margin:0;
  min-height:100vh;
  display:flex;
  justify-content:center;
  align-items:center;
  background: var(--bg);
  color: #0b1220;
}

.container {
  width:100%;
  max-width:900px;
  padding:20px;
  display:flex;
  flex-direction:column;
  gap:20px; /* spacing between cards */
}

.card {
  background: var(--card);
  border-radius: var(--radius);
  padding: 32px;
  box-shadow: 0 8px 22px rgba(9,30,66,0.06);
  text-align:center;
}

h1 {
  font-size:20px;
  color: var(--accent); /* blue headings */
  margin-bottom:20px;
}

form {
  margin-bottom: 10px;
}

button {
  background: var(--accent);
  color: #fff;
  border: none;
  padding: 12px 20px;
  border-radius: var(--radius);
  font-weight: 600;
  cursor: pointer;
  font-size: 16px;
  transition: background 0.2s;
  width: 100%;
  max-width: 280px; /* same size buttons */
}

button:hover {
  background: #094fae;
}

.desc {
  font-size: 12px;
  color: var(--muted); /* tiny grey letters */
  margin-bottom: 16px;
}
</style>
</head>
<body>

<div class="container">
  <!-- Teacher Portal Card -->
  <div class="card">
    <h1>Teacher Portal</h1>
    <form action="/time_table" method="get">
      <button type="submit">Create Time Table</button>
    </form>
    <p class="desc">Generate and manage class schedules</p>
  </div>

  <!-- Student Portal Card -->
  <div class="card">
    <h1>Student Portal</h1>
    <form action="/display" method="get">
      <button type="submit">Display Time Table</button>
    </form>
    <p class="desc">View your personalized class schedule</p>

    <form action="/tasks">
      <button type="submit">View Tasks</button>
    </form>
    <p class="desc">Check your pending and completed tasks</p>
  </div>
</div>

</body>
</html>
